config:
  target: 'http://localhost:3000/song'
  phases:
    - duration: 120
      arrivalRate: 350
  processor: '../helpers/powerCurve.js'
scenarios:
  - flow:
    - function: 'randomSongPath'
    - get:
        url: '{{ songPath }}'
